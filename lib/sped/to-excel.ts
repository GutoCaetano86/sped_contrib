// AST -> XLSX. Ver docs/SPEC.md secao 5.3 (tarefa F2-T1).
//
// Contrato previsto: gerarExcel(res: ResultadoParse, layout: Layout): Promise<Buffer>
//
// exceljs em modo streaming (WorkbookWriter); todas as celulas de dados
// formatadas como TEXTO (numFmt '@').

export {};
