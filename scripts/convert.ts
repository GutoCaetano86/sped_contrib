#!/usr/bin/env tsx
// CLI interna de conversao — ver docs/PROMPTS-CLAUDE-CODE.md, tarefa F2-T4.
//
//   npm run convert -- entrada.txt  --saida saida.xlsx
//   npm run convert -- entrada.xlsx --saida saida.txt

function main(): void {
  console.error('CLI de conversao ainda nao implementada (tarefa F2-T4).');
  process.exitCode = 1;
}

main();
